export interface ISprint {
    id: string;
    name?: string;
    startDate?: Date;
    endDate?: Date;
    completeDate?: Date;
    goal?: string;
    description?: string;
    originBoardId?: number;
}
